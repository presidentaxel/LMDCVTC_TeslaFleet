# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Redis (optionnel si vous utilisez Supabase)
REDIS_URL=redis://localhost:6379/0
# Ou pour le dev sans Redis:
# REDIS_URL=memory://dev

# Supabase Configuration (alternative à Redis)
# Pour utiliser Supabase au lieu de Redis, configurez ces variables:
# SUPABASE_URL=https://votre-projet.supabase.co
# SUPABASE_KEY=votre-service-role-key
# SUPABASE_TOKENS_TABLE=tokens
# TOKEN_STORE_TYPE=supabase

# Choix du store: "redis", "supabase", ou "memory" (défaut)
TOKEN_STORE_TYPE=memory

# Tesla Partner Credentials (M2M)
TESLA_CLIENT_ID=your_partner_client_id
TESLA_CLIENT_SECRET=your_partner_client_secret

# Tesla Third-Party OAuth
TP_CLIENT_ID=your_third_party_client_id
TP_CLIENT_SECRET=your_third_party_client_secret
TP_REDIRECT_URI=http://localhost:5173/auth/callback

# Frontend URL
FRONTEND_URL=http://localhost:5173

# CORS Origins (séparées par des virgules)
# Pour le développement, inclure les IPs réseau (ex: http://172.22.176.1:5173)
# En production, utiliser uniquement vos domaines
# Note: En mode dev (ENV=dev), toutes les origines sont acceptées
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173

# Private Key Path (optionnel pour le dev)
PRIVATE_KEY_PATH=./app/keys/private/private_key.pem

# Public Key URL
PUBLIC_KEY_URL=http://localhost:8000/.well-known/appspecific/com.tesla.3p.public-key.pem

# Tesla Region (eu or na)
TESLA_REGION=eu

# Environment (dev, staging, prod)
# En dev, CORS accepte toutes les origines pour faciliter le développement
ENV=dev

