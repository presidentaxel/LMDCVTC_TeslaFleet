# Copier vers .env à la racine du projet (non versionné) avant :
#   docker compose -f docker-compose.prod.yml up -d --build
#
# URL de l'API telle que le navigateur l'appellera (HTTPS, même domaine que l'UI).

VITE_API_BASE=https://teslapi.axelproject.fr/api
